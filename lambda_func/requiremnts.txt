# Lambda deployment package dependencies
# Install with: pip install -r requirements.txt -t ./package/

# HTTP client — used by WorkdayClient to call the Workday RaaS API
requests==2.32.3

# AWS SDK — used by S3Stager and SecretsHelper
# Note: boto3 is pre-installed in the Lambda runtime but pinning it
# here ensures consistent behaviour in local development and CI.
boto3==1.34.144
botocore==1.34.144

# urllib3 is a requests dependency — pin to avoid version conflicts
urllib3==2.2.2

# Snowflake connector — used by SnowflakeStateManager to query the
# audit table for last-run state and write run records after each load
snowflake-connector-python==3.10.0

# Parquet serialisation — used by S3Stager to convert API records to
# typed columnar Parquet files before staging to S3
pandas==2.2.2
pyarrow==16.1.0
